package com.coremasterkb.serving.infrastructure;

public class PgConfig {
    // PostgreSQL connection managed by Spring Boot auto-configuration
    // via spring.datasource.* properties in application.yml
}
