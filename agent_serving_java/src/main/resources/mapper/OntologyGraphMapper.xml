<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coremasterkb.serving.mapper.OntologyGraphMapper">

    <resultMap id="ontologyGraphRow"
               type="com.coremasterkb.serving.mapper.result.OntologyGraphRow">
        <result column="id"                   property="id"/>
        <result column="text"                 property="text"/>
        <result column="title"                property="title"/>
        <result column="document_snapshot_id" property="documentSnapshotId"/>
        <result column="block_type"           property="blockType"/>
        <result column="semantic_role"        property="semanticRole"/>
        <result column="unit_type"            property="unitType"/>
        <result column="source_segment_id"    property="sourceSegmentId"/>
        <result column="hop"                  property="hop"/>
        <result column="conf"                 property="conf"/>
    </resultMap>

    <!--
        Entity linking: query entity names -> canonical ontology entity ids.
        Matches on lower(canonical_name) directly, or via the alias dictionary
        (alias_normalized). names must be lower-cased/trimmed on the Java side.
    -->
    <select id="linkEntities" resultType="java.lang.String">
        SELECT DISTINCT e.id
        FROM ontology_entities e
        WHERE e.domain_id = #{domain}
          AND (
              lower(e.canonical_name) IN
              <foreach collection="names" item="n" open="(" separator="," close=")">#{n}</foreach>
              OR EXISTS (
                  SELECT 1 FROM ontology_alias_dictionary a
                  WHERE a.domain_id = e.domain_id
                    AND a.canonical_name = e.canonical_name
                    AND a.alias_normalized IN
                    <foreach collection="names" item="n2" open="(" separator="," close=")">#{n2}</foreach>
              )
          )
    </select>

    <!--
        Bounded multi-hop recall.
          nb      : recursive neighbourhood, cycle-guarded via path array
          nb_agg  : collapse each entity to shortest hop / best path confidence
          join    : entity -> evidence segment -> retrieval unit (same DB), scope-filtered
          group   : collapse a segment's multiple units / an entity's fan-out to one row per unit
        Edge is undirected (head/tail both traversed); nb.conf is the product of edge confidences.
    -->
    <select id="graphRecall" resultMap="ontologyGraphRow">
        WITH RECURSIVE nb(entity_id, hop, conf, path) AS (
            SELECT e.id, 0, CAST(1.0 AS double precision), ARRAY[e.id]
            FROM ontology_entities e
            WHERE e.domain_id = #{domain}
              AND e.id IN
              <foreach collection="seedIds" item="s" open="(" separator="," close=")">#{s}</foreach>
          UNION ALL
            SELECT nxt.next_id,
                   nb.hop + 1,
                   nb.conf * r.confidence,
                   nb.path || nxt.next_id
            FROM nb
            JOIN ontology_entity_relations r
              ON r.domain_id = #{domain}
             AND (r.head_entity_id = nb.entity_id OR r.tail_entity_id = nb.entity_id)
            CROSS JOIN LATERAL (
              SELECT CASE WHEN r.head_entity_id = nb.entity_id
                          THEN r.tail_entity_id ELSE r.head_entity_id END AS next_id
            ) nxt
            WHERE nb.hop &lt; #{maxHop}
              AND r.confidence &gt;= #{minRelConf}
              AND NOT nxt.next_id = ANY(nb.path)
        ),
        nb_agg AS (
            SELECT entity_id, MIN(hop) AS hop, MAX(conf) AS conf
            FROM nb GROUP BY entity_id
        )
        SELECT ru.id,
               ru.text,
               ru.title,
               ru.document_snapshot_id,
               ru.block_type,
               ru.semantic_role,
               ru.unit_type,
               ru.source_segment_id,
               MIN(nb_agg.hop)  AS hop,
               MAX(nb_agg.conf) AS conf
        FROM nb_agg
        JOIN ontology_evidence_nodes ev
          ON ev.domain_id = #{domain}
         AND ev.target_kind = 'entity'
         AND ev.target_id = nb_agg.entity_id
        JOIN asset_retrieval_units ru
          ON ru.source_segment_id = ev.segment_id
         AND ru.document_snapshot_id IN
         <foreach collection="snapshotIds" item="sid" open="(" separator="," close=")">#{sid}</foreach>
        GROUP BY ru.id, ru.text, ru.title, ru.document_snapshot_id,
                 ru.block_type, ru.semantic_role, ru.unit_type, ru.source_segment_id
        ORDER BY MIN(nb_agg.hop) ASC, MAX(nb_agg.conf) DESC
        LIMIT #{limit}
    </select>

</mapper>
