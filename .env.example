# Application
APP_ENV=dev

# PostgreSQL — Mining & Serving 共用（自动建库建表）
PG_HOST=localhost
PG_PORT=5432
PG_DBNAME=coremasterkb
PG_USER=your_pg_user
PG_PASSWORD=your_pg_password
PG_SSLMODE=disable
PG_GSSENCMODE=disable
PG_POOL_MIN=2
PG_POOL_MAX=10

# Database (legacy)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cloud_core_kb

# Embedding — Zhipu AI Embedding-3 (prod mode)
EMBEDDING_API_KEY=your-zhipuai-api-key-here
EMBEDDING_MODEL=embedding-3
EMBEDDING_BASE_URL=https://open.bigmodel.cn/api/paas/v4
EMBEDDING_DIMENSIONS=1024

# Mining pipeline LLM endpoint — 由 scripts/mining_ui.py 与 mining_run() 共同读取
# MINING_LLM_ENABLED=false 时整个 LLM 路径会跳过，全部降级到规则版
# 实际值见下方 "Mining LLM Client" 段
DOMAIN_PACK=cloud_core_network
MAX_WORKERS=4

# LLM Service (for mining pipeline: question generation, enrichment, discourse analysis)
LLM_SERVICE_PROVIDER_BASE_URL=https://api.deepseek.com/chat/completions
LLM_SERVICE_PROVIDER_API_KEY=sk-your-api-key-here
LLM_SERVICE_PROVIDER_MODEL=deepseek-chat
LLM_SERVICE_PROVIDER_BYPASS_PROXY=false

# LLM Service model endpoints (for shared embedding / rerank capabilities)
# In the current project setup, embedding and rerank use the same BigModel key.
LLM_SERVICE_EMBEDDING_BASE_URL=https://open.bigmodel.cn/api/paas/v4/embeddings
LLM_SERVICE_EMBEDDING_API_KEY=your-zhipuai-api-key-here
LLM_SERVICE_EMBEDDING_MODEL=embedding-3
LLM_SERVICE_EMBEDDING_DIMENSIONS=1024
LLM_SERVICE_RERANK_BASE_URL=https://open.bigmodel.cn/api/paas/v4/rerank
LLM_SERVICE_RERANK_API_KEY=your-zhipuai-api-key-here
LLM_SERVICE_RERANK_MODEL=rerank

# LLM Service — internal network headers (only for embedding / rerank)
# Uncomment and fill when deploying to internal network with gateway auth.
# When empty or absent, external network behavior is unchanged.
# LLM_SERVICE_MODEL_EXTRA_HEADERS={"X-HW-ID": "your-hw-id", "X-HW-APPKEY": "your-hw-appkey"}

# Serving direct fallback (only used when llm_service shared model endpoints are unavailable)
RERANK_API_KEY=your-zhipuai-api-key-here
RERANK_BASE_URL=https://open.bigmodel.cn/api/paas/v4
RERANK_MODEL=rerank

# Mining LLM Client (read by scripts/mining_ui.py)
LLM_SERVICE_URL=http://localhost:8900
MINING_LLM_ENABLED=true
# 通过 scripts/mining_ui.py 启动时默认按 true 处理，便于 localhost 直连
MINING_LLM_BYPASS_PROXY=true
